export type PageContext = {
  canonicalPath: string;
  description: string;
  robots: string;
  title: string;
};

export type ProductInfo = {
  details: string[];
  inventoryStatus: string;
  name: string;
  price: string;
};
